# 安全政策

## 敏感信息保护

本仓库严格遵守以下安全实践：

### API 密钥管理
- **绝不硬编码 API Key**：所有密钥通过环境变量或配置文件管理
- **本地存储**：用户密钥仅存储在本地，不提交到 Git
- **示例模板**：README 使用占位符而非真实密钥

### .gitignore 配置
已排除以下敏感文件：
```
data.json          # 用户配置和密钥
.env               # 环境变量
.env.local         # 本地环境变量
node_modules/      # 依赖包
*.log              # 日志文件
.DS_Store          # macOS 系统文件
```

## 代码安全

### 输入验证
- 所有用户输入经过转义处理
- 使用 `escapeHtml()` 防止 XSS 攻击
- 对 AI API 响应进行类型检查

### 权限控制
- 最小权限原则
- iframe 使用 sandbox 限制
- 仅请求必要的 workspace 访问

## 依赖安全

定期运行以下命令检查依赖漏洞：
```bash
# Node.js 项目
npm audit

# Python 项目
pip audit
```

## 报告漏洞

如发现安全漏洞，请通过以下渠道报告：
- GitHub Issues（标记为安全相关问题）
- 联系维护者

## 安全审计日志

| 日期 | 审计内容 | 结果 |
|------|----------|------|
| 2026-08-25 | 仓库初始安全审计 | ✅ 通过 |

---

*本安全政策会根据项目发展持续更新*
